export interface ErrorSetter {
    errors: object,
}

export interface UserLoginInfo{
    username: string;
    password: string;
}

// interface Props {
//     handleEvent(values :UserLoginInfo): void;
// }
